---
description: Run the Gamba Pick bot
---

1. Run the bot
```bash
uv run python scrapling_pick.py --headless --summarize
```

2. (Optional) Run with specific currency only
```bash
uv run python scrapling_pick.py --headless --summarize --only LTC
```
